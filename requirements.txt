# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Local AI Models
faster-whisper>=1.0.0              # Local Whisper STT (tiny model for dev)
requests>=2.31.0                   # To call Colab-hosted LLM/TTS APIs

# RAG Pipeline
chromadb>=0.5.0                    # Vector database (runs locally)
sentence-transformers>=2.7.0       # Text → embedding conversion

# Database
sqlalchemy>=2.0.0                  # Python ORM for PostgreSQL
psycopg2-binary>=2.9.0             # PostgreSQL driver
alembic>=1.13.0                    # Database migrations

# Audio Processing
pydub>=0.25.1                      # Format conversion (requires ffmpeg)

# Text-to-Speech (lightweight, runs on CPU)
gTTS>=2.5.0                        # Free fallback TTS (no cloning)

# Configuration
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# Utilities
loguru>=0.7.0
pyyaml>=6.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
